package battle;

//戦闘後の経験値処理
public class skilltree_action {
	/*
	public static void mantra_exp(Player[] player, Enemy enemy) {
		for (int i = 0; i < player.length; i++) {
			int get_exp=enemy.get_Lv()*10;
			if (player[i].get_hunting()) {
				get_exp=get_exp*2;
			}
			player[i].get_now_mantra().set_exp(player[i].get_now_mantra().get_exp() +get_exp);
			if(player[i].get_now_mantra().get_exp()>=player[i].get_now_mantra().get_max_exp()) {
				//
			}
		}
		
	}
	public static void mantra_menu() {
		
	}
	*/
}
